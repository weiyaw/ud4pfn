defaults:
  - _self_

rep_dir: null  # path to the rep_dir to process
bootstrap_samples: 1000
seed_offset: 10391
overwrite: false

lastrun: ${now:%Y-%m-%dT%H:%M:%S%z}
git: ${githash:}

hydra:
  run:
    dir: "${rep_dir}"
