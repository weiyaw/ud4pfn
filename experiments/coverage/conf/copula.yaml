defaults:
  - _self_

rep_dir: null
rollout_times: 200
rollout_length: 1000
seed: 29427
overwrite: false
lastrun: ${now:%Y-%m-%dT%H:%M:%S%z}
git: ${githash:}

hydra:
  output_subdir: null
  run:
    dir: "${rep_dir}"
